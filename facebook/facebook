import pandas as pd

df = pd.read_csv("dataset_facebook.csv", delimiter=";")

print(df)

subset1 = df[['Type', 'Category', 'Post Month']]

print(subset1.head())

subset2 = df[['Type', 'Category', 'comment']]

print(subset2.head())

merged = pd.concat([subset1, subset2], axis=1)

print(merged)

sorted_df = merged.sort_values(by=['comment'], ascending=False)

print(sorted_df)

print(merged.T)

print(merged.transpose())

print(df.shape)

import numpy as np

arr = np.array(df[['Post Month', 'Post Weekday']].head(6))

reshaped = arr.reshape(3, 4)

print(reshaped)
